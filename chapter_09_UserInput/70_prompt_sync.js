//prompt-sync is a package that allows you to get user input in a synchronous manner in Node.js. 
// //It provides a simple way to prompt the user for input and wait for their response before proceeding with the rest of the code.

// const prompt = require("prompt-sync")();

// let num = Number(prompt("Enter a number: "));

// if (num % 2 === 0) {
//     console.log(num + " is Even");
// } else {
//     console.log(num + " is Odd");
// }