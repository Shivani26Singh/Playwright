let no_audi = null;
let blank;
let age = 0;
let audi = "";

console.log(typeof no_audi);   // "object" (null is considered an object in JavaScript)
console.log(typeof blank);     // "undefined" (blank is declared but not assigned, so it's undefined)
console.log(typeof audi);      // "string" (empty string)
console.log(typeof age);       // "number" (0 is a number)